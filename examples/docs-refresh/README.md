# Example: Docs Refresh

This example shows a small documentation workflow:

- a single worker is refreshing product documentation;
- the truth surface tracks one bounded task;
- the hard gate checks for real output instead of note churn;
- the handoff note names the exact next file to open.

Use this example when you want to apply the playbook to documentation, migration, or knowledge-base work.
